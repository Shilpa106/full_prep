--  SELECT * FROM EMPLOYEE11

/*
SELECT LAST_NAME,HIRE_DATE
FROM EMPLOYEE11
WHERE HIRE_DATE=TO_DATE('January 25,2005','fxMonth DD, YYYY');
*/
-- alter table employee11 add column DEPT_ID INT;
-- UPDATE EMPLOYEE11 SET DEPT_ID=1 WHERE EMPLOYEE_ID=100;
-- UPDATE EMPLOYEE11 SET DEPT_ID=2 WHERE EMPLOYEE_ID=101;
-- UPDATE EMPLOYEE11 SET DEPT_ID=3 WHERE EMPLOYEE_ID=102;

-- SELECT * FROM EMPLOYEE11
/* CASE STATEMENT */
/*
SELECT FIRST_NAME,DEPT_ID,SALARY,
  CASE DEPT_ID WHEN 1 THEN 1.5*SALARY
                     WHEN 2 THEN 2.0*SALARY
  ELSE SALARY
  END "REVISED SALARY"
  
FROM EMPLOYEE11
*/

/* DECODE */
/*
SELECT FIRST_NAME,DEPT_ID,SALARY, 
       DECODE(DEPT_ID,1, 1.5*SALARY,
			           2, 2.0*SALARY,
			   SALARY)
			   "REVISED SALARY"
FROM EMPLOYEE11;
*/

/* COALESCE */
-- SELECT LAST_NAME FROM EMPLOYEE11;

-- SELECT COALESCE(LAST_NAME, '- NA -') FROM EMPLOYEE11;
-- UPDATE EMPLOYEE11 SET LAST_NAME='null' WHERE EMPLOYEE_ID=101
-- SELECT * FROM EMPLOYEE11

SELECT GREATEST('XYZ','xyz') from dual;

























